\chapter{Introduction}
\label{ch:intro}

\section{Background}
\label{background}

IRs (Intermediary Representations) are used across different stages of compilation 
in order to work at different levels of abstraction. This allows high level 
optimizations to be applied on a high level view of the code, while letting low 
level optimizations (like instruction selection) be handled close to the ISA. 

There is a lot of work needed to optimize these IRs but if programs can be proven equivalent 
(for example, by leveraging formal semantics) a technique arises called superoptimization. 
By enumerating all programs of a small size we may find a shorter program that is equivalent 
to a larger one  a larger one\textsuperscript{citation needed}. This technique can be applied 
across IRs, the target could even be an ISA. Therefore we can use superoptimization to find lowerings 
from a source IR (like LLVM IR) to an assembly language like RISC-V. Hence I'll refer to 
this as backend synthesis.

\section{Motivation}

Why go through the effort of providing formal semantics for your IR when you could just
go ahead and write the lowering yourself? Well, manually written backends are quite 
buggy. Additionally, with proper tools and support compiler developers will have an easier time
defining the semantics of their IRs.

\begin{figure}[H] 
    \centering
    \includegraphics[width=1\textwidth]{intro/BugsInLLVM.png}
    \caption{The overall evolution of bugs in the LLVM repository. The area in gray shows
		new bugs reported every month. \cite{compiler-bugs}}
    \label{fig:bugs_llvm}
\end{figure}
\stefan{Don't know if this is the best graph to show backend bugs}

Luckily, there exist verification tools that can prove the correctness of a lowering for 
which semantics are available \cite{smt-mlir}. However these require the compiler 
developer to both specify the semantics for their source and target representations, while 
also providing the lowering themselves. This is increases the overall amount of work 
required of the compiler developer.


The aim of this project is to give motivation for compiler developers to provide formal
bitvector semantics for their IRs by synthesizing a large portion of the backend. The
synthesized lowering will also verified so no additional work will be required for correctness.
Moreover, the synthesized lowering will use the minimal number of assembly instruction for
each IR instruction to achieve high performance.

Thus this project introduces a prototype of backend synthesis to a subset RISC-V, 
evaluating its effectiveness, as well as some optimization techniques that can be 
applied to speed up the process, achieving more coverage of the source representation,
and a more performant backend, all while being proven correct.


\section{Approach}

For easier development, prototyping and experimenting I will be using the 
xDSL\textsuperscript{citation needed} framework which contains an implementation
of a RISC-V dialect, as well as making use of the infrastructure in the open source project 
xdsl-smt, an implementation of SMTLib for xDSL, and mlir-fuzz, which is used for 
enumerating mlir programs. 

The project consists \stefan{WIP} of a series of contributions to these projects to add
lower the RISC-V dialect to the smt dialect and formally prove equivalence to some 
authoritative RISC-V semantics, enhance mlir-fuzz to generate RISC-V programs, then run
the enumerative synthesis, generating a backend for LLVM IR, evaluate the coverage and 
performance of the synthesized lowering.

Following this the aim is to experiment with various optimizations to increase synthesis
speed, increase coverage or performance.\stefan{Experiments and optimizations are WIP}

\section{Previous Work}
This project is based of work done by my supervisor Mathieu Fehr \stefan{there should
probably be more credits here} on xdsl-smt, and mlir-fuzz. It uses open source 
lean semantics\textsuperscript{citation needed} proven equivalent to the authoritative SAIL 
semantics\textsuperscript{citation needed}. 
Superoptimization is a well known technique utilized successfully in projects such as
Souper \cite{souper}.


\clearpage
\chapter{Preparation}
\label{ch:prep}
\section{MLIR}

MLIR (Multi-Level Intermediate Representation) is an extensible compiler framework that allows
users to define IRs named dialects. Developed as a sub-project of LLVM it aims to facilitate the 
construction of domain specific compilers and improve compilation for heterogeneous computing.
One feature worth highlighting is the iteroperability of dialects, allowing any dialect to be
used within any MLIR program.


The main computation unit in MLIR programs are operations, which have a set of operands (variables),
attributes (static data), and results. Programs are a series of operations which can come from 
multiple dialects, they also have types, which are verified to ensure the program is well formed. 
%
In order to make dataflow information explicit, which makes optimization much easier, MLIR enforces
single static assignment, that is each value is defined exactly once (as a result of an operation), 
and will never be reassigned.
%
In the example below we can see \textcolor{red}{result(s)}, 
\textcolor{blue}{operation name}, \textcolor{green!50!black}{operands}, \textcolor{orange}{attributes}, 
as well as \textcolor{teal}{operation type}, and \textcolor{magenta}{result type}:

\begin{minted}[escapeinside=||]{text}
|\textcolor{red}{\texttt{\%res}}| = |\textcolor{blue}{\texttt{"arith.addi"}}|(|\textcolor{green!50!black}{\texttt{\%a, \%b}}|) |\textcolor{orange}{\texttt{\{overflowFlags = 0 : i64\}}}| : |\textcolor{teal}{\texttt{(i32, i32)}}| -> |\textcolor{magenta}{\texttt{i32}}|
\end{minted}



\subsection*{xDSL}

Troughout the project I will be using the xDSL compiler framework \cite{xdsl} which is 
interoperable with MLIR, but is written in Python to allow for rapid prototyping. The IR files for
both xdsl and mlir both use the same format, so existing tools build for MLIR can still be used.
Moreover, because of the aforementioned interoperability, if the prototype tool has succesful
results, a performant version can be implemented in C++ within the MLIR ecosystem.

\subsection*{LLVM IR}

\fehr{Sentence is broken}
LLVM is set of compiler and technologies that leverages a language-independent 
intermediate representation (LLVM IR). The IR serves as a portable high level assembly language
that is a common target for optimizations. Many modern programming languages (C++, Haskell, Rust,
etc.) have frontends that target LLVM IR. This is why, I've elected to use it as the source IR 
for this project.

In particular, I will be targeting a subset of the LLVM dialect, which mimicks the
LLVM IR while having access to the suite of tools provided by MLIR and xDSL. I'm focusing on
the arithmetic subset, as these isntructions can be easily modeled with bitvector SMT semantics.
Here is an example of a function that checks whether their arguments are equal:
\begin{minted}{mlir}
func.func @main(%arg0 : i64, %arg1 : i64) -> i1 {
    %0 = llvm.icmp eq, %arg0, %arg1 : i64
    func.return %0 : i1
 }
\end{minted}


\subsection*{RISC-V}

RISC-V is a simple open source instruction set architecture, with a variety of extensions.
I've chosen it as my target for the project due to it's simplicity and extensibility. 
xDSL has an implementation of a dialect for the RISC-V ISA, which will be used in this project.
%
Below is the lowering of the LLVM dialect presented before in this dialect. It's operations
are equivalent to a RISC-V assembly instructions, but the code is in SSA form.
We can also refer to specific registers as part of the type attribute, although this
is unnecesary and better handled by a register allocator.

\begin{minted}{mlir}
func.func @main(%arg0 : !riscv.reg, %arg1 : !riscv.reg) -> !riscv.reg {
  %0 = riscv.sub %arg0, %arg1 : (!riscv.reg, !riscv.reg) -> !riscv.reg
  %1 = riscv.sltiu %0, 1 : (!riscv.reg) -> !riscv.reg
  func.return %1 : !riscv.reg
}
\end{minted}


\subsection*{IRDL}

IRDL (IR Definition Language) \cite{irdl} is a dialect that is used to define dialects.
This is useful to define the structure of our source and target dialect to a fuzzer,
a tool that will generate programs using the defined operations. A irdl definition looks
like the following, where operands, and results are given type constrains for the operation
to satisfy in order to be verified. In the example below we have to operands that must be
of type \texttt{i64}, and a result of the same type, we also have the operation name \texttt{and}.
\begin{minted}{mlir}
// Structure of an llvm.and operation
irdl.operation @and {
  %integer = irdl.is i64
  irdl.operands(operand0: %integer, operand1: %integer)
  irdl.results(result0: %integer)
}
\end{minted}
\subsection*{PDL}

PDL (Pattern Definition Language) is a dialect used in order to define rewrite
patterns. Such patterns can be used in order to transform code.
%
This will be the primary method used to create a backend as PDL patterns can be applied efficiently
by a generic algorithm, and can be checked for correctness using the tools that we will develop. 
After obtaining a series of PDL rewrites for our IR, we can convert that into a lowering.
%
For example a the PDL pattern below converts the LLVM IR equality example above into the RISC-V 
instructions below:
\begin{minted}{mlir}
pdl.pattern : benefit(1) {
  %0 = pdl.type : i64
  %1 = pdl.operand : %0
  %2 = pdl.operand : %0
  %3 = pdl.attribute = 0 : i64
  %4 = pdl.type : i1
  %5 = pdl.operation "llvm.icmp" (%1, %2 : !pdl.value, !pdl.value)
       {"predicate" = %3} -> (%4 : !pdl.type)
  %6 = pdl.result 0 of %5
  pdl.rewrite %5 {
    %7 = pdl.type : !riscv.reg
    %8 = pdl.operation "riscv.sub" (%1, %2 : !pdl.value, !pdl.value) 
        -> (%7 : !pdl.type)
    %9 = pdl.result 0 of %8
    %10 = pdl.attribute = 1 : si12
    %11 = pdl.operation "riscv.sltiu" (%9 : !pdl.value) {"immediate" = %10} 
        -> (%7 : !pdl.type)
    %12 = pdl.result 0 of %11
    pdl.replace %5 with (%12 : !pdl.value)
  }
}
\end{minted}

\section{Semantics}

At the base of the entire project sits the idea of formal semantics for our dialects.
We need to express how our instructions act on their inputs, and we want the semantics
to be easy to work with using automatic solvers. Therefore we use SMT formulas and model
our instructions as bitvector transformations. We also primarily care about basic arithmetic
operations so bitvector theory seems like a natural choice.

\subsection{SMT dialect}

In order to remain within the confines of MLIR, we leverage the specialized SMT dialect 
\cite{smt-mlir} to define the semantics of our IR instructions. This dialect can be 
easily converted to SMT-Lib, a format that is widely used by many SMT solvers. Moreover
we can use xDSL infrastructure to aid with parsing, printing and introspection. 
%
Below is an example of the semantics of the \texttt{riscv.sltw} instruction. The more complex
behaviour of the instruction (which isn't really relevant for this example) gets broken down 
into simpler primitives which a SMT solver can work with.
\begin{minted}{mlir}
%0 = "smt.bv.extract"(%arg0) {start = #builtin.int<0>, end = #builtin.int<31>} 
    : (!smt.bv<64>) -> !smt.bv<32>
%1 = "smt.bv.extract"(%arg1) {start = #builtin.int<0>, end = #builtin.int<4>} 
    : (!smt.bv<64>) -> !smt.bv<5>
%2 = "smt.bv.zero_extend"(%1) : (!smt.bv<5>) -> !smt.bv<32>
%3 = "smt.bv.shl"(%0, %2) : (!smt.bv<32>, !smt.bv<32>) -> !smt.bv<32>
%4 = "smt.bv.sign_extend"(%3) : (!smt.bv<32>) -> !smt.bv<64>
\end{minted}

\subsection{Refinements}

Dialects don't always work on the same types of values (RISC-V dialect uses registers, and LLVM IR 
uses integers), checking if a lowering is ``correct'' isn't as easy as saying that for all inputs
the programs yield the same outputs. In fact we need to introduce the following \emph{refinement}
relation. A program is a \emph{refinement} of another if the following proposition holds:
\begin{equation*}
\text{\emph{target inputs refine the source inputs}} \implies \text{\emph{target outputs refine the source outputs}}
\end{equation*}

This way we can meanigfully talk about such lowerings. Notice that what it means for a value to 
\emph{refine} another is up to the implementation, however a clear example arises when considering 
poison in LLVM. Poison is a form of undefined behaviour that propagates, i.e. if any of the operands
of an operation are posion, than so is the result, while some operations generate poison (like an 
overflowing integer addition). This is useful because it can be used to optimize certain code sequences,
as we can always assume that poisoned values are convenable for optimization, since they are the
result of undefined behaviour. Looking at the following example, which checks if $x+1 > x$:

\begin{minted}{mlir}
llvm.func @f(%x: i32) -> i1 {
  %one = llvm.mlir.constant(1 : i32) : i32
  %add = "llvm.add"(%x, %one) {overflowFlags = #llvm.overflow<nsw>} 
      : (i32, i32) -> i32
  %cmp = llvm.icmp "sgt" %add, %x : i32
  llvm.return %cmp : i1
}
\end{minted}

Because of poison, this code can be optimized to, as we can ignore the undefined behaviour that
arises when $x = \texttt{INT\_MAX}$ by simply asserting that the poisoned value is \texttt{true}:
\begin{minted}{mlir}
llvm.func @f(%x: i32) -> i1 {
  %one = llvm.mlir.constant(1 : i1) : i1
  llvm.return %one : i1
}
\end{minted}

We can now see an useful refinement for poison: Any value \emph{refines} poison.

\subsection{Verifying rewrites}
One benefit of having access to SMT semantics for our dialect is leveraging SMT solvers to 
prove properties about different expressions. For our purposes we prove that our output program 
is a refinement of our input program. So we can exhaustively search the program space until
an equivalent program is found. This also gives us confidence in the correctness of our lowering,
as the SMT solver ensures that the translation is valid for all inputs.
%
There are more benefits of having such semantics, as presented in \cite{smt-mlir}, however these
aren't the focus of the project.

\section{Superoptimization and Lowering Synthesis}

%\fehr{You talk about superoptimization but the section is about lowering synthesis.}
As mentioned in \ref{background}, superoptimization is a technique where a compiler 
attempts to find the optimal sequence of instructions that has the same semantics as a source
sequence. One technique used to do this is via exhausting search over the program space and boolean 
tests to prove equivalence of the source and target.
%
Superoptimization has been used to successfully find new optimizations both offline, and to improve
programs by running it as a compilation pass. Some very popular compilers have 
implemented and used this technique, like gnu superoptimizer for x86 assembly \cite{gnu-superopt},
and souper for LLVM IR \cite{souper}.


Lowering synthesis is a similar concept, but instead of searching for optimizations it searches
for valid lowerings between dialects. This is the novel idea behind the project, with the aim of
evaluating the efficiency and exploring the benefits of this technique.
%
The premise is simple, incur one large computational cost once for each source-target pair 
in order to search and find a safe lowering. This is obviously useful when compiling
to low level dialects that need to be lowered to various ISAs.
%
This reduces the number of bugs, but it also opens the door for the easy extension of the 
infrastructure with superoptimizers and other verification tools.

\subsection*{MLIR-fuzz}

In order to perform the enumerative search I use MLIR-fuzz \cite{mlir-fuzz}.
This tool, written in C++, uses the IRDL specification of a dialect in order to 
enumerate all the programs with certain characterstics (number of free variables,
number of instructions, etc.).
%
I extend the tool with the RISC-V dialect, including adding the ability to handle
constants that are passed in as attributes (to allow the generaliztion of immediates).
Thus the tool enumerates risc-v programs with empty slots for the immediates that
the SMT solver fills with values that can make the programs equivalent.

\section{Formal Verification}

%\fehr{People might not know what bitblastable means here.}
In this work, we establish some semantics for RISC-V, so we can talk about the correctness
of a lowering. We take as a source of truth, semantics defined in the sail language \cite{sail-paper}.
Because we will be using these semantics to automatically generate proofs using traditional tools
like SAT and SMT solvers we these to be bitblastable, i.e the bit vector expressiosn can be converted
into expressions about individual bits before being passed into these tools. In particular, we 
use hand-written semantics in Lean, that are proven equivalent to the source of truth. \cite{riscv-lean}
%
Lean is a proof assistant which is used to obtain machine verified theorems. In order to achieve
a satisfactory level of correctness for our semantics we require Lean theorems that prove the
equivalences between our established RISC-V semantics and those we deem correct. 

To achieve this we built tools to convert between SMT dialect semantics and bitblastable lean 
functions we we then used to write the desired proofs. This gives us great confidence in the 
correctness of our semantics which we use as part of our lowering synthesis process.



\section{Requirements Analysis}

We established the following series of requirments for the project, in order to build a succesful
implementation plan:

\begin{itemize}
  
\item Building a proper understanding of the mlir and xdsl frameworks. 
Thus some of my time will be spent researching how mlir works, and how to work within the 
framework.

\item Implement semantics for RISC-V dialect as a lowering to the SMT dialect. 

\item Build a tool to convert the smt semantics implemented above into Lean theorems. 
Formally prove the correctness of the semantics.

\item Modify mlir-fuzz to properly enumerate RISC-V program. Add handling for attribute immediates
while enumerating risc-v programs.

\item Write code to synthesize a backend to RISC-V, collecting relevant data.

\end{itemize}

\subsection{Evaluation questions}
\stefan{I'll write this section, after I write evaluation, it should be a mirror
posing which questions are to be asked, and evaluated in the project.}

\section{Software Engineering}

\subsection{Development Model}

Due to the novel nature of the project, development followed the spiral model, which focuses on 
iteration and risk assesment.
%
Therefore a working prototype was implemented with some limited scope. Upon completion requirments
were be reevaluated and the scope was apropriatly expanded, and the process was repeated. 
The project went through the following major iteration cycles:
\begin{enumerate}

\item Working end-to-end prototype for basic instructions

\item Expanded prototype with more complex instructions

\item Handling attributes and immediates

\item Optimized  using heuristics
  
\end{enumerate}

\subsection{Languages and Tools}

The project was written primarily in Python, within the VSCode editor with the pylance extension,
which provides strict type checking. In addition, the project also used C++, for the work performed
on mlir-fuzz, which was compiled using clang and ninja.
%
Throughout the project I've extensively used the MLIR and xDSL frameworks, while working on the
xdsl-smt project (containing the majority of the project's code) and mlir-fuzz (containg the 
code for program enumeration and exhaustive search).
%
Due to the open source nature of the projectgit and GitHub were used for source control.

\subsection{Development Environment}
This subsection will include the specs of my laptop and the PC I'm doing evaluation on.

\subsection{Open Source Contributions}

The project was be built on top of the open source project xdsl-smt \cite{xdsl-smt}.
As such the contributions (were)/(will be) upstreamed. As a result there was additional attention 
given to code clarity and conciseness. This also means that a good understanding of the xdsl-smt
project was required to not reimplement features and be able to leverage already existing code 
effectively.

This was different that working on a solo project, while also providing valuable experience
for working in industry and other research projects. Focus on clear and clean code should make
it easier to maintain and build upon in the future.

\section{Starting Point}

As a starting point I only have knowledge that is considered covered in the courses in Part IA and IB
of the tripos, with some additional experience working on C++ projects.
Moreover I had no prior knowledge of the xdsl-smt and mlir-fuzz projects before I started development.

\clearpage
\chapter{Implementation}
\label{ch:impl}
This project implements the systems necessary to perform lowering synthesis from the 
LLVM IR dialect of MLIR to the RISC-V dialect present in xdsl-smt.

\fehr{Probably keep this as a paragraph instead of a bullet point list}
\begin{itemize}
\item In section \ref{RISC-V to SMT} we present an overview of converting RISC-V dialect operations into 
their underlying SMT semantics using the smt dialect.
\item To ensure the correctness of our semantics we built a tool to generate Lean theorems
of equivalece between our selected source of truth and our implementation, which are then
checked for correctness by the proof-assistant. This is covered in section \ref{lean theorems}.
\item We then present (in section \ref{enumeration}) the modificatioes made to the enumerator that were made to allow us
to exhaustively search the RISC-V program space and handle the way attributes are used in the
RISC-V dialect.
\item Then, in section \ref{backend synthesis}, we cover the backend synthesis process, from the 
search for a valid transformation, to the construction of a PDL pattern for it, that will be used
as part of the lowering.
\end{itemize}

\section{Base RISC-V to SMT Semantics}
\label{RISC-V to SMT}

In order to lower a program written in the RISC-V dialect to its SMT semantics, we need to
be able to lower the types of all the primitive values (registers and immediates) together 
with all of the instructions as a sequence of SMT instructions. Thus we need to model for
the RISC-V assmenbly.


\subsection{Converting MLIR Types}

There are only a few types of values we will encounter in RISC-V code, given we are working 
with values in CPU registers, and encoded within instructions they can all be represented as 
bitvectors of apropriate sizes, however they will need to be extended to the same size for 
our smt operations. Register values will be encoded as 64-bit bitvectors, and the 12 bit
immediates and 6 bit shift value attributes that appear in the RISC-V instructions are 
extended to 64 bits to operate together with the registers.

\subsection{Handling diffrent Instruction Types}

RISC-V instructions can be divided into a few diffrent types, based on their structure.
Therefore we can extract the values, apply the appropriate conversion, then use the values
for the SMT semantics.

In our implemntation we handle the following instruction categories: 

\fehr{Either expand these paragraphs to 2-3 sentences, or merge these into a single paragraph}
\paragraph{Register-Register Operations} 
We can directly get the values of the registers and convert them to 64 bit bitvectors.

\paragraph{Register-Register Word Operation} 
Since we are using RV64, we also have some operations that are meant to work on a 32-bit word.

The semantics for this are the same as for Register-Register Operations, but the values are 
first truncated to 32 bits, and extended back at the end of the operation.

Example: 
\begin{minted}{mlir}
%op1 =  smt.bv.extract %rs1 from 0 : (!smt.bv<64>) -> !smt.bv<32>
%op2 =  smt.bv.extract %rs2 from 0 : (!smt.bv<64>) -> !smt.bv<32>
// Perform the operation on the truncated operands, yielding %tmp
%res = smt.bv.sign_extend %tmp : (!smt.bv<12>) -> !smt.bv<64>
\end{minted}

\paragraph{Register-Immediate Operation}
We extract the immediate and extend it to a full sized bit vector, which can then operate 
with the register.

Example: 
\begin{minted}{mlir}
%op1 = %rs1
%op2 = smt.bv.sign_extend %imm : (!smt.bv<12>) -> !smt.bv<64>
// Perform the operation on the operands
\end{minted}

\paragraph{Register-Immediate Word Operation}
We sign extend the immediate to 64 bits, than truncate the result, before sign extending it again.

Example: 
\begin{minted}{mlir}
%op1 = %rs1
%op2 = smt.bv.sign_extend %imm : (!smt.bv<12>) -> !smt.bv<64>
// Perform the operation on the operands, yielding %tmp
%tmp2 = smt.bv.extract %rs2 from 0 : (!smt.bv<64>) -> !smt.bv<32>
%res = smt.bv.sign_extend %tmp2 : (!smt.bv<32>) -> !smt.bv<64>
\end{minted}

\paragraph{Register-Register Shift (Word) Operation}
We truncate the shift register (since only the lower bits matter), and then zero-extend it back 
to perform the operation.

Example for 64 bits:
\begin{minted}{mlir}
%op1 = %rs1
%tmp = smt.bv.extract %rs2 from 0 : (!smt.bv<64>) -> !smt.bv<6>
%op2 = smt.bv.zero_extend %tmp : (!smt.bv<6>) -> !smt.bv<64>
//Perform the operation, using op2 as the shift value
\end{minted}

For word operations we use 32 bit bitvectors, truncate rs1, and sign extend the result.

\paragraph{Register-Shamt Shift (Word) Operation} 
Extend the shift value from 6 bits the 64 (32) bits.

Example for 64 bits: 
\begin{minted}{mlir}
%op1 = %rs
%op2 = smt.bv.zero_extend %tmp : (!smt.bv<6>) -> !smt.bv<64>
//Perform the operation
\end{minted}

Again, word operations will have additional truncation of \texttt{\%rs} followed by sign extension of \texttt{\%res}.


\subsection{Exploiting identical semantics}

Across different instruction types we encounter instructions with the same underlying behaviour.
Take for example \texttt{riscv.add} and \texttt{riscv.addi}. Their behaviour can be expressed as a single
\texttt{smt.bv.add} opreration on the relevant values. So, as long as we are careful on how we extract
the values from each instruction we can exploit this similarity so have cleaner and more concise code.

Moreover, we also see how many semantics would only differ by the smt operation they perform.
Thus we recognize the following categories:

\fehr{Same here about merging paragraphs or expanding them}
\fehr{It would be nice to have a table the number of instructions in each category.
Possibly the list of instructions per category as well if it fits?}


\paragraph{Trivial Operations}
These operations can be expressed as a single smt operation.

\begin{minted}{mlir}
// obtain op1 and op2 as SSA Values
%res = smt.OPERATION %op1 %op2 : (!smt.bv<?>, !smt.bv<?>) -> !smt.bv<?>
// convert res to !smt.bv<64>
\end{minted}

The way we obtain the operands and process the result is defined by the instruction type, and
they type of \texttt{?} can be inferred from the types of the operands.

This is a the common case and includes a wide range of operations.


\paragraph{Comparisons}

These require special care as the smt dialect comparison operations yield boolean values
that need to be converted into bitvectors. So semantics for comparison operations look like the
this:

\begin{minted}{mlir}
// obtain op1 and op2 as SSA Values
%res_bool = smt.COMPARISON op1 op2 : (!smt.bv<?>, !smt.bv<?>) -> !smt.bv<?>
%zero = smt.bv.constant #smt.bv<0> : !smt.bv<64>
%one = smt.bv.constant #smt.bv<1> : !smt.bv<64>
%res = "smt.ite"(%res_bool, %one, %zero) :
    (!smt.bool, !smt.bv<64>, !smt.bv<64>) -> !smt.bv<64>
\end{minted}

\paragraph{Other}

Some instructions have completely custom semantics, however with careful implementation
they plugged into the existing instruction types. For example the the \texttt{slli.uw} instruction
which has the following semantics, requiring a trunctation followed by a zero extension:

%\fehr{Here it would be really nice to have an example for instance.}
\begin{minted}{mlir}
%trunc_reg = smt.bv.extract %reg : (!smt.bv<64>) -> !smt.bv<32>
%ext_reg = smt.bv.extend %trunc_reg : !smt.bv<32> -> !smt.bv<64>
%res = smt.bv.shl %ext_reg %shamt : (!smt.bv<64>, !smt.bv<64>) -> !smt.bv<64>
\end{minted}

By building abstractions for these instruction types and common behaviours, we can very easily
implement the semantics for an instruction. This yields an easily extensible framework for 
defining semantics for RISC-V in the smt dialect.

\subsection{Leveraging xdsl-smt}

We use tools provided by xdsl and xdsl smt to create mlir blocks like the examples I've shown
above for every operation and SSA value. After that, I register my lowerings to xdsl-smt and
a lowering pass is created that recursively lowers all of the instructions from a source program
into a program using the smt dialect.

%\fehr{Maybe a paragarph or a diagram to explain how this actually works?}
%Agreed, will do diagram later

\begin{verbatim}
          Diagram showing the lowering process 
\end{verbatim}


The smt program is then converted into a SMTLib S-expression, which will can be used by SMT solvers
in a variety of tools, in particular we care about validating the equivalence between different
such expressions.

\section{Theorems of equivalence in Lean}

\label{lean theorems}

In order to ensure the correctness of our semantics we programatically create lean4 theorems
to formally prove the equivalence of our semantics to those we consider a source of truth, which
are already written in lean.

We first obtain Lean functions that are representative of our RISC-V semantics, and we use them
as part of our equivalence theorems. First we encode our risc-v instructions into mlir blocks, 
with the arguments being the registers and immediates used by the instruction. Using our lowering to the smt dialect previously defined,
we first lower the block, obtaining the semantics of our instruction. 
%
All that's left is lowering from smt to a lean function, but since lean isn't an mlir
dialect we cannot use xdsl to our advantage when outputing lean code, so we also need to write 
working formatting for our lean functions and theorems.

\subsection{RISC-V to Lean Functions}

Walking through an example, we first start with a RISC-V instruction:
\begin{minted}{mlir}
%0 = riscv.slt %arg0, %arg1 : (!riscv.reg, !riscv.reg) -> !riscv.reg 
\end{minted}

Which gets lowered to semantics:
\begin{minted}{mlir}
%0 = "smt.bv.slt"(%arg0, %arg1) : 
   (!smt.bv<64>, !smt.bv<64>) -> !smt.bool
%1 = smt.bv.constant #smt.bv<0> : !smt.bv<64>
%2 = smt.bv.constant #smt.bv<1> : !smt.bv<64>
%3 = "smt.ite"(%0, %2, %1) : 
   (!smt.bool, !smt.bv<64>, !smt.bv<64>) -> !smt.bv<64>
\end{minted}

The next step is converting the semantics above into a lean function like the following:
\begin{minted}{lean}
let a0 : Bool := (BitVec.slt arg1 arg0) ;
let a1 : BitVec 64 := 0 ;
let a2 : BitVec 64 := 1 ;
let a3 : BitVec 64 := (cond a0 a2 a1) ;
\end{minted}

Thus our program is a series of let bindings for each operation and SSA value. We assign each
value a different ID and convert each element of the source program to a lean equivalent.
%
Types and attributes are straightforward, a \texttt{!smt.bv<X>} gets lowered to \texttt{BitVec X}
constant attributes get lowered directly, and their type is luckily inferred by the lean type checker.
Booleans behave in the same way.
%
We also have a variety of operations, some of which are grouped together when lowering so we can leverage
the similarity of their structure.

\paragraph{Constant Operation}
The source operation:
\begin{minted}{MLIR}
%res = smt.bv.constant #smt.bv<X> : !smt.bv<Y>
\end{minted}
is lowered to:
\begin{minted}{lean}
let res : BitVec Y := X ;
\end{minted}


\paragraph{Standard Binary Operation}
The source operation:
\begin{minted}{mlir}
%res = "smt.OP"(%arg0, %arg1) : (?, ?) -> ResultType
\end{minted}
is converted to:
\begin{minted}[escapeinside=||]{lean}
let res : |\textcolor{cyan}{ResultType}| := |\textcolor{cyan}{OP arg0 arg1}|;
\end{minted}
Where we use \textcolor{cyan}{cyan} to represent the lowered types, arguments and operation names.
%
Examples of binary operations are: \texttt{smt.bv.add, smt.bv.slt, smt.bv.ult} and more, all of which
have a lean equivelent like \texttt{BitVec.add, BitVec.slt, BitVec.ult}.

\paragraph{Shift Operation}
Lean bit vector shift operations use shift values of type \texttt{Nat} so we need to add a 
conversion for shift operations, so:
\begin{minted}{mlir}
%res = "smt.OP"(%arg0, %arg1) : (?, ?) -> ResultType
\end{minted}
is converted to:
\begin{minted}[escapeinside=||]{lean}
let res : |\textcolor{cyan}{ResultType}| := |\textcolor{cyan}{OP arg0}| (BitVec.toNat |\textcolor{cyan}{arg1}|);
\end{minted}

This pattern is used for operations \texttt{smt.bv.shl, smt.bv.lshr} and \texttt{smt.bv.ashr} which have
bitvector equivalents \texttt{BitVec.shiftLeft, BitVec.ushiftRight} and \\ \texttt{BitVec.sshiftRight} respectively

\paragraph{Ternary Operation}
\begin{minted}{mlir}
%res = "smt.OP"(%arg0, %arg1, %arg2) : (?, ?, ?) -> ResultType
\end{minted}
is converted to:
\begin{minted}[escapeinside=||]{lean}
let res : |\textcolor{cyan}{ResultType}| := |\textcolor{cyan}{OP arg0 arg1 arg2}|;
\end{minted}

This pattern is used for operation \texttt{smt.ite} which has the
lean equivalent \texttt{cond}.

\paragraph{Extend Operation}

\begin{minted}{mlir}
%res = "smt.extendOP"(%arg) : (?) -> !smt.bv<X>
\end{minted}
is converted to:
\begin{minted}[escapeinside=||]{lean}
let res : |\textcolor{cyan}{ResultType}| := Bitvec.|\textcolor{cyan}{some}|Extend X |\textcolor{cyan}{arg}|;
\end{minted}
Where \emph{\texttt{start}} and \emph{\texttt{end}} are attributes of the source operation.

This pattern is used for operation \texttt{smt.bv.zeroExtend} and \texttt{smt.bv.signExtend}.

\paragraph{Extract Operation}

\begin{minted}{mlir}
%res = smt.bv.extract %arg from ? : (?) -> ResultType
\end{minted}
is converted to:
\begin{minted}[escapeinside=||]{lean}
let res : |\textcolor{cyan}{ResultType}| := BitVec.extractLsb |\emph{start} \emph{end} \textcolor{cyan}{arg}|;
\end{minted}
Where \emph{\texttt{start}} and \emph{\texttt{end}} are attributes of the source operation.

We use the patterns above to recursively lowers all operations from the semantics block into an 
appropriate Lean function.

%\fehr{bv\_decide is creating a proof, not really a theorem? Maybe "Generating automatically proven theorems"?}
\subsection{Proving theorems with bv\_decide}

After succesfully obtaining a lean function for the RISC-V instructions semantics we formulate 
equivalence theorems like the following, usinging the bv\_decide tactic, which leverages the 
bitblastable nature of the semantics and invokes a SAT solver to automatically generate a proof
for the theorem, which gets checked by Lean.

The theorems will take the same shape, first using the unfold tactic to replace the let bindings
with their underlying expressions, followed by bv\_decide, which finishes the proof:
\begin{minted}{lean}
theorem OP_eq (a0 : BitVec 64) (a1 : BitVec 64) 
    : RV64.OP a0 a1 = _OP a0 a1 := by
  unfold [RV64.OP, _OP]
  bv_decide
\end{minted}
Here, \texttt{\_OP} is the name of the function we automatically printed, and \texttt{RV64.OP} is
the functions which we consider a source of truth.

\section{RISC-V Enumeration}

\label{enumeration}

To achive our goal of backend synthesis we require a method of enumerating the RISC-V program space.
Thus we levereged and modified the mlir-fuzz tool to allow for exhaustive enumeration of RISC-V programs.
This was done by implementing the dialect in IRDL, to provide mlir-fuzz with the operation definitions, 
and extending the enumeration with `synthetic operations' which have `holes' for attributes 

\subsection{RISC-V in IRDL}

The RISC-V dialect is a work in progress implementation as part of the xDSL project, therefore
when working with mlir-fuzz, which is a project written in C++ working with MLIR, we used
opaque types when refering to registers. This is achieved by adding a new configuration for 
the dialect in mlir-fuzz, and handling the edge cases with care.

Example of a basic operation defined in IRDL:
\begin{minted}{mlir}
irdl.operation @add {
  %register = irdl.is !riscv.reg
  irdl.operands(operand0: %register, operand1: %register)
  irdl.results(result0: %register)
}
\end{minted}

\subsection{Synthetic Operations}

Many RISC-V operations use attributes to encode constants. During our synthesis process we want
to be able to infer these constants, therefore when enumerating we are going to replace operations
that use attributes with a `synthetic form'. These \texttt{synth.op}s contain the information to build 
an instance of the underlying operation.

\begin{Verbatim}[commandchars=\\\{\}, fontsize=\small]
\textcolor{red}{%res} = "synth.op"(\textcolor{green!50!black}{%op...}) <\textcolor{blue}{op_name = \emph{name}}> \{\textcolor{orange}{attr : type, ...}\} : \textcolor{teal}{(!type)} -> \textcolor{magenta}{!type}
\end{Verbatim}

As we can see in the example above a synthetic op contains the \textcolor{blue}{original operation name},
\textcolor{green!50!black}{operands}, \textcolor{orange}{a dictionary containing attribute names and 
their desired types}, \textcolor{teal}{the operand types} and \textcolor{magenta}{the result types}.

To obtain the attribute types for emmiting synth ops, we would need to parse them from the IRDL constraints.
Unfortunately, IRDL doesn't yet have the expressivity to handle arbitrary types for
attributes. As a workaround we resorted to using a fixed value for immediates (usually zero)
so the operation has the correct shape, which would be ignored when emmiting synthetic operations.
To identify which operations are meant to be emmited using this method we marked them with the IRDL
attribute \texttt{synth}.
\begin{minted}{mlir}
irdl.operation @addi attributes {synth} {
  %register = irdl.is !riscv.reg
  %imm = irdl.is 0 : si12 
  irdl.operands(operand0: %register)
  irdl.results(result0: %register)
  irdl.attributes {"immediate" = %imm}
}
\end{minted}

\fehr{Maybe first explain why we need synthetic operations? What is the problem you are
trying to solve here?}
Synthetic operation allow for the usage of arbitrary smt constants for the values that would
normally be represented by the attributes. For example we have the following lowering from
a synthetic add to the semantics, where we use a \texttt{smt.declare\_const} instead of the
attribute.

\begin{minted}{mlir}
%res = "synth.op"(%op) <op_name = riscv.addi> {immediate : si12} 
    : (!riscv.reg) -> !riscv.reg
\end{minted}
To:
\stefan{Don't know if I should tackle reconstruction here on in the next chapter, for now I'll
leave a comment here and explain later in the next chapter, since this one is mostly about enumeration}
\begin{minted}{mlir}
%imm = smt.declare_const : () -> !smt.bv<12>
//... (some additional operations used to reconstruct the transformation)
%imm_ext = smt.bv.signExtend %imm : !smt.bv<12> -> !smt.bv<64>
%res = smt.bv.add %op %imm_ext : (!smt.bv<64>, !smt.bv<64>) -> !smt.bv<64>
\end{minted}
This leaves a hole, that the smt solver will be able to fill with an appropriate constant if one
exists that leads to a correct lowering.

\section{Backend Synthesis}

\label{backend synthesis}

After leveraging mlir-fuzz to exhaustivly search the program space we need to leverage the
semantics to check if the input and output programs are equivalent.

We handle synth ops here, converting them into their underlying operation, but leaving
marker operations (synth.FromValueOp, and synth.ConvOp) to later reconcile the values
from given in the SMT model with the attributes in our program.

Example of transformation the program goes trough.

Performance concerns about using 64-bit registers followed by hypothesis that the restriction
is the size of the immediates, not of the registers, which is defined by the architecture, and
is a manageable size.

\subsection{Refinements}

As mentioned in preparation there are different refinements that we can consider.

Explanation of sign extension refinement.

Pros and cons analysis (some fast comparisons, other operations may require handling, as they may
overflow, signedness/unsignedess is weird).

Explanation of trunc refinement.

Pros and cons (arithmetic is clear, comparisons become much harder to handle).

Implement and test both refinements.

\subsection{Multithreaded Iterative Deepening}

Enumerate all input instructions, and wrap them into functions. Each individual instruction
is can be handled individually.

For each input instruction perform iterative deepening search of the program space.

Store the results, with additional data for further analysis.

\subsection{Conversion to PDL}
The functions obtained from the enumerative search are then converted into PDL Patterns.

Explain the conversion process, with an example.

\section{Pruning the searchspace}
This section will detail attempts at optimization that will hopefully succeed.

I'll add more in this section, after implementing heuristic search.

\section{Repository Overview}

This page will consist of a table of all the relevant files, with a short description.

\clearpage
\chapter{Evaluation}
\label{ch:eval}
\section{Correctness}
\fehr{What might be helpful here is a list of research questions. Not sure if this is expected or not for these kind of theses, but
that's usually quite helpful in papers at least.}
\stefan{I'm not writing bullet points here, as it's mostly going to be graphs, and results of
analysis}

\subsection{Semantics correctness}
The semantics are proven equivalent to authoritative SAIL semantics in Lean.

\subsection{PDL Transformations}
The pdl transformations are proven equivalent using an smt solver.

\section{Synthesis duration analysis}

\subsection{Choosing Refinements}

\subsection{Effects of TBD optimization}

\section{Coverage of LLVM IR instructions}

\subsection{Base RISC-V instruction set}

\subsection{Bit Manipulation Extension}

\subsection{Multiplication and Division Extension}

\section{Performance against LLVM}




\clearpage
\chapter{Conclusion}
\label{ch:conc}

\section{Work Completed}

\subsection{Synthesized large chunk of LLVM to RISC-V Backend}

\subsection{Examined optimizations techniques for backend synthesis}

\subsection{Novel Contribution}
Exploration of backend synthesis as a viable alternative to manually written lowerings.

Analysis of different refinements, and their effect

\section{Future Work}
Creation of a proper tool written in
a performance oriented language like C++.

Evaluating the performance of techniques to prune the search space via dataflow analysis.

Using LLMs to attempt to search for lowerings of up to one size large than we searched for
for the input instructions we failed to find.

\section{Reflections}
Working on an open source project, and how it's different from a personal project.

Learning about important frameworks to compiler development like MLIR and LLVM.

Learning about formal verification, and using it in a real project.

Working on an open research project great, learning how research in academia is like.



